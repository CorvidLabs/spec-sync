# spec-sync v4 — generated by `specsync migrate`
# Committed: config.toml, registry.toml, lifecycle/, changes/, archive/
# Ignored: backups (migration artifact), hashes (cache)

backup-3x/
hashes.json
