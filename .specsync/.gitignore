# spec-sync v4 — generated by `specsync migrate`
# Committed: config.toml, registry.toml, lifecycle/, changes/
# Ignored: backups, archive (large/low-value for public repos)

backup-3x/
archive/
hashes.json
