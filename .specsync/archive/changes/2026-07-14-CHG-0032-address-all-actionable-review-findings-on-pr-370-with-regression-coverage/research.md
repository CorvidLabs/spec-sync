---
change: CHG-0032-address-all-actionable-review-findings-on-pr-370-with-regression-coverage
artifact: research
---

# Research

The sequence ledger is a time-indexed claim, so a historical reconstruction may include only collision acknowledgements whose numeric sequence existed at that point. Cargo package selection accepts `-p`, `--package`, and `--package=...`; selecting SpecSync through any of them invokes the same lifecycle CLI even when the default `check` subcommand is implicit. Registry-backed modules resolve to two canonical files: the registered spec and its sibling `requirements.md`.
