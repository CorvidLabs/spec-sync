---
change: CHG-0106-make-verification-currency-a-content-question-delete-the-git-ancestry-walk-the
artifact: research
---

# Research

- `verification_is_current_checked_with_project_digest` — the fusion point; content checks and
  the persistence walk confirmed separable before editing.
- `verify_change_locked` — `workspace_digest: project_input_digest(root)?`, confirming the two
  names are one computation.
- `validate_verification_for_commit_binding` — content half plus a commit-identity/ancestry
  arm; both call sites retain `current_commit` for other purposes.
- `verification_commit_is_accepted_current` — used by trusted transitions and accepted-evidence
  authentication; out of scope for this step.
- Sandbox drill 008 established the pre-change squash behaviour before the edit, so the
  post-change difference is measured rather than asserted.
