## ADDED

### REQUIREMENT REQ-cmd-coverage-002

`coverage` SHALL report an unmeasured tree as unmeasured in every format.

Acceptance Criteria
- The JSON payload uses `null` rather than a substituted percentage.
