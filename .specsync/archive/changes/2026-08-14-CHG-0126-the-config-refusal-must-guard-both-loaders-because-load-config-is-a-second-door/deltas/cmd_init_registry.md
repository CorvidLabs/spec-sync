## ADDED

### REQUIREMENT REQ-cmd-init-registry-003

The registry initialiser SHALL keep working over a broken configuration.

Acceptance Criteria
- It requests the permissive loader explicitly, for the same reason as the wizard.
