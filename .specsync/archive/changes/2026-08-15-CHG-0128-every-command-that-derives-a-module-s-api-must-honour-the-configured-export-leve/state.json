{
  "schema_version": 1,
  "workflow_version": 2,
  "workflow_origin_version": 2,
  "id": "CHG-0128-every-command-that-derives-a-module-s-api-must-honour-the-configured-export-leve",
  "slug": "every-command-that-derives-a-module-s-api-must-honour-the-configured-export-leve",
  "title": "Every command that derives a module's API must honour the configured export level and parse mode, so check, score, new, generate, scaffold and diff cannot disagree about what the API is",
  "description": "Every command that derives a module's API must honour the configured export level and parse mode, so check, score, new, generate, scaffold and diff cannot disagree about what the API is",
  "kind": "bug_fix",
  "state": "archived",
  "canonical_applied": true,
  "base_commit": "48caf5688f9e0b7b2096b33c8e6a5ae47897daa4",
  "created_at": 1786812758,
  "updated_at": 1786823277,
  "affected_specs": [
    "scoring",
    "generator",
    "exports",
    "cmd_new",
    "cmd_scaffold",
    "cmd_diff"
  ],
  "affected_paths": [
    "src/scoring.rs",
    "src/generator.rs",
    "src/exports/mod.rs",
    "src/commands/new.rs",
    "src/commands/scaffold.rs",
    "src/commands/diff.rs",
    "CHANGELOG.md",
    ".specsync/change-sequence.json"
  ],
  "no_spec_change": false,
  "no_spec_change_rationale": null,
  "acceptance_criteria": [
    "On a project configuring export_level = type, score grades the API dimension against the type-level surface that check validates, so the two commands agree about what the module's API is. new and generate produce specs whose Public API contains only symbols check will accept, so the tool cannot generate work its own validator rejects. diff reports drift only for symbols in the configured surface. The configured parse mode is threaded to the same sites, so an AST project gets AST parsing everywhere rather than in check alone. A project on the default member level and regex mode behaves byte-identically to before."
  ],
  "selected_artifacts": [
    "context",
    "testing",
    "tasks",
    "design",
    "requirements",
    "docs",
    "research",
    "plan"
  ],
  "dependencies": [],
  "answers": {
    "architecture_risk": "yes",
    "public_contract": "yes"
  }
}
