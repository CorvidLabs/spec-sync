## ADDED

### REQUIREMENT REQ-cli-args-014

The strict-score flag SHALL document its inclusive boundary and the directory consequence at the point of use.

Acceptance Criteria
- The help text states that omitting an explicit floor implies eighty and that eighty passes.
- It states that a `files:` entry naming a directory scores zero and therefore fails the floor, so a reader learns the interaction without reading the scoring module.
