# Documentation

No user-facing documentation changes. The behaviour this restores is the one the command already
claimed: `Next:` names a runnable command, and evidence is reported where it exists.

The reasoning lives at the sites that enforce it — why the ship lane may narrow but not
contradict, why the blocker arm was removed, why `find_change_dir` is the resolver to reuse, and
why the archived reads are lenient.
