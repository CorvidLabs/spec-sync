# Tasks

- [x] Land drill 053's evidence assertions first, red (sandbox #90)
- [x] Enumerate every hard-coded active-workspace path in `commands/` — 2 production, 2 in tests
- [x] Expose `find_change_dir` and route both evidence reads through it
- [x] Make the archived reads lenient so a damaged repository still renders
- [x] Make `ship_next` defer to `lifecycle_next` outside the shipping window
- [x] Drop the blocker-restatement arm entirely
- [x] Add four unit tests; confirm which discriminate and which are controls
- [x] Run drill 053 against fixed and a separate unfixed checkout
- [x] Whole board
