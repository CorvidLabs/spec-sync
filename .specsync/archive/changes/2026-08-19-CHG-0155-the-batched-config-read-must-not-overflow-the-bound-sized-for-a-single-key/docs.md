# Documentation

No user-facing documentation changes. The values derived and the errors surfaced are unchanged;
only the size of an internal read bound moves.

The reasoning is recorded at the call site: why the bound was wrong, what it is now sized
against, and which sibling read it matches.
