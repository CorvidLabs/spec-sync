# Docs

No user-facing documentation changes. Nothing here alters a command, a flag, an output format,
or a file layout; the observable difference is that two files a newer 6.x wrote no longer stop
an older one, and a policy written before a field existed still loads.

The changelog entry belongs under `[Unreleased]` beside CHG-0157's, since this corrects what
that entry claimed.
