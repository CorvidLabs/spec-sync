# Tasks

- [x] Enumerate every `succession_change_key` caller before changing any
- [x] Measure supersedes and succession-tuple digest exposure across the archive
- [x] `happens_after` replaces the name-derived key
- [x] Three sorts aligned with `approved_scope`
- [x] Three ordering comparisons made chronological
- [x] Strict-sort message rewritten to describe the ordering actually used
- [x] Tests discriminate through the public path, with a vacuity control
