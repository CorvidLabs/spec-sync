---
change: complete-specsync-6-promotion-and-public-release-contracts
artifact: requirements
---

# Requirements

The existing exact two-platform qualification policy is authoritative. Promotion and publication must consume exactly that evidence set and retain all identity, outcome, duplicate, immutable-tag, and provenance checks. The fix must not bypass a failed qualification or relax rulesets.

Public claims must match implementation: reviewer labels and stored provider fields bind claims/content and do not authenticate a person or fetch a live GitHub verdict during local finalization. Identity/provenance enforcement requires separately configured hosted policy. Existing human review, blocking verdict, append-only history, and freshness rules remain unchanged.

README and CLI examples must parse with the6.x binary and show scope approval, check, human review, immediate review/finalize, and same-PR archive before merge. SECURITY will explicitly cover6.x including RCs without silently retiring existing5.x support. Consolidate6.0 notes under the6.0 heading and leave Unreleased for future changes; do not fabricate a publication date or claim an unpublished stable tag exists.
