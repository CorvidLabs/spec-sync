---
change: close-the-specsync-6-0-0-p1-release-defects-found-in-overnight-proving
artifact: tasks
---

# Tasks

- [x] Bundle lifecycle limits under `src/` and pin identity with the CI copy.
- [x] Move the empty-attempts guard outside `exists()`; refuse a missing ledger while Verifying.
- [x] Fold CRLF in `canonical_definition_artifact_payload`.
- [x] `new` uses `generate_spec` and scaffold name rules.
- [x] JSON parse-fail sets `load_error`.
- [x] Sanitize git children; confine `scaffold --dir`; fail-closed generate skips.
- [x] Fence-aware Public API reader and `--fix` heading scan (byte-preserving blanking).
- [x] `check --spec` flag; README/site/cli/mcp-security/CHANGELOG.
- [x] Tests for each P1.
- [x] Definition approved by 0xLeif; scoped check, review, and same-PR finalization follow on the PR.
