---
change: CHG-0068-stabilize-specsync-6-0-with-a-low-churn-normal-workflow-preserved-audited-guara
artifact: context
---

# Context

SpecSync 5.2 requires definition and closing approvals and usually repeats the full product matrix
for lifecycle-only follow-up commits. The current post-merge archive model adds a second code PR
even when no product files change. CorvidLabs/rune PR #23 is the compatibility case: archiving eight
already-accepted changes touches 103 files and repeats review and CI.

The open PR backlog also shares a strict-check failure mode: implementation tests pass, but old
branches lack an active change whose scope covers their meaningful paths. Large lifecycle branches
then amplify the problem by repeatedly scanning Git history and accepted evidence.

SpecSync 6.0 has one workflow and one layout for every change. A human approves scope once. Code,
canonical specs, requirements, and tests are implemented and checked bidirectionally. Agent-authored
work receives one independent scoped review. `change finalize` then creates a metadata/archive-only
commit on the same PR, and GitHub remains responsible for merge protections and the merge itself.

The archive-only commit must not pay for the product matrix again. Its lane proves that the parent
implementation commit already has the required green checks, the finalization diff contains only
approved lifecycle/archive paths, code/spec/tree relationships are unchanged, and archive ownership
and integrity remain valid. Explicit `--strict`, project policy, or release/security classification
adds the existing powerful full-history, full-suite, security, and release validators to the same
evidence and layout. Strict validation never changes approvals, state transitions, commands,
finalization, archival, or the newcomer workflow.

The approval-boundary regression discovered during implementation is resolved inside this same
workflow: the original human event binds the stable intent and affected scope, while a separate
execution digest binds artifacts, deltas, tests, canonical materialization, and lifecycle metadata.
The original broad CHG-0068 digest preimage was never committed, so the ledger now states
`source_preimage_status: unavailable` and `equivalence_claim: none` instead of pretending the new
stable projection can reproduce it. The source approval event remains untouched. A CHG-0068-only
compile-time allowlist freezes the exact historical commit/blob, source event, adopted scope
digest, authorization, and classification digest. Any projection rewrite requires a visible
security-sensitive code change plus fresh validators and independent review; no second approval is
invented.

Independent review found and drove four further hardening changes: scope removal is renewal-worthy,
review evidence records `pass` or `block` and rejects the scope approver, freshness inspects every
descendant against every parent so change/revert history cannot reuse review, and archive
finalization resumes safely when a crash leaves the accepted package at its dated destination.
Hosted routing now hashes the v2 execution digest/verdict exactly like Rust, preserves v1 archives
on full validation, and runs fork reviews read-only.

## Material lessons

| Fingerprint | Trigger and root cause | Durable invariant | Regression |
|---|---|---|---|
| `change/approval-projection/renewal-conflict` | A renewed direct approval retained the one-time CHG-0068 adoption, creating two competing projections; status also read only direct projections. | Direct renewal supersedes legacy adoption, and status explains drift from either valid projection. | `renewed_direct_scope_approval_supersedes_legacy_adoption`; current CHG-0068 status projection check. |
| `ci/review-freshness/stale-suppression` | Hosted routing treated matching contract/workspace digests as current despite a stale execution digest, reviewer, verdict, or change/revert history. | Review reuse requires schema 2, `pass`, independent reviewer, all three digest matches, an ancestor implementation commit, and bounded every-parent freshness. | `.github/scripts/test-classify-ci-paths.sh` stale-execution, self-review, and change/revert cases. |
| `change/archive-retry/date-rollover` | Post-move recovery recomputed the archive directory from the current date, so a retry after midnight lost the moved package. | Recovery resumes from the uniquely discovered existing dated destination; wall-clock date is only used for a new move. | `post_move_same_pr_finalization_resumes_across_dates_without_another_approval`. |
| `github/finalize-history/unbounded-query` | Hosted every-parent review traversal had no output, time, descendant, or parent bound. | Finalization history queries fail closed at explicit resource limits while retaining every-parent inspection. | `.github/scripts/test-lifecycle-workflows.sh` bounded-query assertions. |
| `change/approval-docs/contract-drift` | Newcomer docs said every artifact edit needs approval, contradicting the stable-scope projection. | Docs distinguish stable intent/contract/scope renewal from automated execution/evidence refresh. | Site lint, tests, and build plus canonical approval-boundary requirements. |
| `ci/test-isolation/host-marker-leak` | A nested local lifecycle test removed `CI` but inherited `GITHUB_ACTIONS`, accidentally enabling hosted-only checks. | Tests that model local execution clear every recognized hosted-CI marker. | `comment_suppresses_configured_command_output_but_check_streams_it` under inherited `CI=true GITHUB_ACTIONS=true`. |
| `change/adoption-anchor/unavailable-replay` | The one-time CHG-0068 exception skipped lineage/blob authentication when its anchor object was absent. | Adoption fails closed unless the exact allowlisted commit, parent, and approvals blob resolve from full trusted history. | `scope_adoption_fails_closed_when_anchor_is_unavailable_or_replayed`. |
| `change/review/self-attested-identity` | A mutable reviewer string could impersonate independence and overwrite a block. | Reviewer claims are stable ASCII, every attempt is append-only, and merge authority comes from the exact GitHub Actions check proven on the implementation parent. | Independent/block/pass trail test plus hosted provenance/ledger assertions. |
| `change/finalization/unreachable-implementation` | Squash/rebase merge can discard reviewed commits and collapse a valid block→pass trail into one archive introduction. | Reachable history keeps strict one-at-a-time appends; a collapsed introduction is accepted only at a terminal v2 archive whose finalization digest binds the latest review and exact clean archive subtree. | `workflow_v2_archive_survives_squash_merge_in_fresh_clone` with block→pass history. |
| `change/archive/partial-terminal-publication` | A kill after writing archived `state.json` but before `change.md` could strand a mixed-state package. | Terminal files share the transaction journal, and finalize recovers it before reading lifecycle state. | Cross-date retry test injects a journaled post-state-write interruption. |
| `change/review-bounds/local-hosted-divergence` | Native and hosted freshness validators used different descendant/parent limits. | Rust, classifier, and finalizer load one committed limits document; output/time limits are streamed and fail closed. | Classifier/workflow parity assertions and native change/revert coverage. |
| `change/review/block-erasure` | A later pass could replace a committed blocking attempt if validators checked only the latest projection. | Every review child is one strict append and finalization preserves the exact parent ledger; readers validate every attempt. | `scoped_review_attempt_history_rejects_erasing_a_committed_block` plus hosted workflow assertions. |
| `github/review/block-pass-deadlock` | A pass child tried to reuse the blocked parent’s failed scoped-review check even though implementation had not changed. | Appended reviews preserve the reviewed implementation ancestor; hosted recovery proves bounded metadata-only ancestry and reuses only that ancestor’s implementation/trust checks, while the current job is the new review check. | Native block→pass identity assertion and real M/M classifier/workflow harness. |
| `github/workflow/heredoc-import-scope` | Bounded-query imports were added to a neighboring embedded Python program, leaving the archive validator unable to execute its selector loop. | Workflow self-tests anchor required imports and limits inside the exact archive-validation step that uses them. | `.github/scripts/test-lifecycle-workflows.sh` scoped heredoc assertion. |
| `change/workflow-version/downgrade` | Omitting the defaulted workflow version could route a v2 change through legacy commands and hide a downgrade after a revert. | Every new change records an immutable workflow-origin version, and bounded every-parent history rejects removal, downgrade, or later concealment. | `workflow_v2_cannot_downgrade_by_omitting_workflow_version`; `workflow_version_downgrade_then_revert_remains_invalid`. |
| `change/archive/introduction-rewrite` | A fresh clone could treat a later rewritten archive tree as its own historical anchor after source commits were discarded. | Native validation requires one non-root archive introduction absent from every resolvable parent; post-merge metadata additionally binds that source introduction, finalization digest, merge commit, and archive tree for release reconstruction. | Fresh-clone rewrite assertion plus post-merge/release workflow self-tests. |
| `change/transaction/torn-journal` | Direct journal/payload writes and lossy backup reads could turn a crash or unreadable target into partial publication or destructive recovery. | Publish a digested transaction envelope durably before payloads, distinguish not-found from read failure, replace/restore atomically, sync directory renames, and leave malformed journals untouched. | Torn canonical/staging journal, unreadable backup, rollback, recovery, and archive post-move tests. |
| `github/trust/pr-head-policy` | The optimized classifier/finalizer and parent-check provenance named a PR workflow path without proving its immutable revision. | A SHA-pinned `pull_request_target` guard inspects candidate Git objects without checking them out, blocks trust-policy edits, and publishes an exact revision-bound check; the first merged guard is a full-CI independently reviewed bootstrap. | Trusted-policy workflow assertions and exact event/revision/PR/head verifier checks. |
| `github/archive/fork-publication` | A merged fork ran post-merge publication under the read-only `pull_request` token, so the required archive binding/check/comment could not be written. | Post-merge publication runs from the immutable base-controlled `pull_request_target` workflow and fetches candidate/merge identities only as Git objects. | Workflow harness requires the closed target event, base checkout, object-only fetch, and forbids PR-head/merge checkout. |
| `github/archive/unbounded-release-history` | Post-merge and release reconstruction repeated unbounded archive-introduction Git scans outside the shared limits. | One protected verifier streams bounded Git output, caps reachable commits and introduction parents, and rejects later archive-tree rewrites; release Git queries use the same output/time limits. | Synthetic introduction/successor/rewrite/history-cap tests plus workflow resource-bound assertions. |
| `github/trust/bootstrap-self-authorization` | A generic missing-base-guard exception accepted any candidate that merely supplied its own policy files. | The one bootstrap is frozen to CorvidLabs/spec-sync PR #480, exact base/ref/head branch metadata, ancestry, and a required newly-added file set; no later repository/PR/base can enter it. | Arbitrary missing-guard repository/base fixture is rejected; workflow harness asserts every frozen identity. |
| `change/workflow-v1/first-introduction` | Removing both workflow-version fields before the first commit defaulted a new v2 change to v1 with no parent transition to expose the downgrade. | Workflow-v2 adoption creates one immutable pre-v2 cutoff; v1 eligibility requires that exact ID/version at the cutoff, and active/global checks validate the history rather than hiding invalid records. | `first_reachable_workflow_v1_state_requires_the_trusted_pre_v2_cutoff` exercises status, legacy accept/archive, and global check. |
| `github/trust/check-production-surface` | The guard blocked enumerated workflows but allowed a new workflow or local Action to publish an indistinguishable required context. | Every `.github/workflows/*.yml`, `.yaml`, and `.github/actions/**` change blocks optimized reuse; policy changes require the separately pinned full path. | Workflow harness requires the broad workflow/local-Action patterns rather than selected filenames. |
| `change/workflow-baseline/squash-parent` | The first baseline used the branch tip as its cutoff, so a squash introduction no longer had that commit as a reachable parent. | Adoption binds the trusted comparison-base ancestor; introduction requires that cutoff to precede its first parent, and the immutable baseline survives squash/rebase. | `workflow_v2_archive_survives_squash_merge_in_fresh_clone` now commits work before adoption and validates the squashed fresh clone. |
| `github/trust/local-action-descendant` | An end-anchored `\.github/actions(/|$)` branch matched the directory but not files below it; rename detection could also hide the deleted protected path. | The guard matches `.github/actions/**` and diffs with `--no-renames`, so add, modify, delete, and both sides of a move cannot bypass the policy boundary. | Lifecycle harness builds a Git fixture and exercises protected A/M/D/rename paths through the exact workflow regex. |
| `change/workflow-v1/explicit-origin` | Legitimate v1 records created after origin anchoring persist `workflow_origin_version: 1`, but cutoff eligibility accepted only omitted origins. | Trusted-cutoff v1 compatibility accepts only the same ID with version 1 and origin omitted or explicitly 1. | `workflow_v2_adoption_keeps_explicitly_anchored_workflow_v1_records_readable`. |
| `github/archive/mutable-action-ref` | The privileged post-merge publisher checked out its immutable workflow revision through a mutable Action tag. | Privileged base-controlled workflows pin every executable Action dependency to a full commit SHA. | Lifecycle harness requires the full checkout SHA in the post-merge target workflow. |
| `change/status/missing-history-stderr` | Sandbox legacy records referenced discarded base commits; expected ancestry probes inherited Git's raw fatal stderr before otherwise-valid status JSON. | Boolean ancestry probes suppress child stderr and surface only SpecSync-owned status or remediation text. | Private sandbox `change status --json` returns the three exact accepted records with no raw Git diagnostics. |
| `change/sequence-collision/historical-owner-substitution` | Three accepted sandbox records with one acknowledged numeric collision had all signed the exact committed collision-owner ledger; reconstruction instead substituted each record's own ID after a later sequence claim. | Historical sequence reconstruction reuses the boundedly discovered canonical committed ledger only when its same-sequence collision explicitly names the record; ordinary and unavailable history retain successor-aware synthetic reconstruction. | `later_sequence_claim_reuses_the_committed_collision_owner_ledger`, `accepted_later_sequence_owner_covers_post_acceptance_collision_acknowledgement`, and the private sandbox v2-adoption drill. |
| `github/trust/root-action-manifest` | CI executes the repository-root composite Action through `uses: ./`, but the trust guard covered only workflows and `.github/actions/**`. | The complete executable Action-definition surface includes root `action.yml`/`action.yaml`; add, modify, delete, and rename all block optimized reuse. | Real Git A/M/D/rename fixtures exercise root `action.yml` through the extracted workflow regex. |
| `change/workflow-history/cross-date-rearchive` | A valid archive→reopen→rearchive moved immutable state from one dated archive path to another, while history validation inspected only the active and latest paths and misread the old path as anchor deletion. | Workflow-origin validation boundedly discovers every reachable canonical dated archive state path for the exact change ID and follows identity across moves. | `workflow_version_history_follows_cross_date_rearchive_paths`; repository CHG-0039 status returns authenticated history. |
| `github/trust/path-record-boundary` | The guard converted NUL-delimited Git names into newline-delimited text before matching, allowing a valid workflow filename containing an embedded newline to split into two unprotected records. | Trust-policy classification preserves Git's NUL record boundaries and applies a byte-pattern full match to each filename independently. | Real Git embedded-newline workflow fixture passes through the exact workflow matcher. |
| `github/archive/rewrite-restore` | Archive verification compared only the introduction and final subtree, so a malicious intermediate rewrite followed by byte restoration disappeared from the net result. | Every bounded archive-path-touching commit and readable parent must retain the introduction tree; deletion, rewrite, or rewritten parent fails closed. | Synthetic rewrite→restore history is rejected by `verify-archive-introduction.py`. |
| `change/workflow-baseline/rewrite-restore` | Baseline validation compared introduction, HEAD, and working-tree bytes but did not inspect intermediate values, allowing rewrite→restore history. | Every bounded baseline-touching commit and readable parent must contain the immutable introduction bytes. | `workflow_v2_baseline_rewrite_then_restore_remains_invalid`. |
| `change/test-fixture/active-lifecycle-coupling` | The adoption-anchor regression embedded CHG-0068's mutable active `state.json` and `approvals.json`; the required direct renewal removed the one-time adoption and same-PR finalization would move both files. | Historical compatibility tests use an immutable, purpose-named fixture and construct only the exact legacy event under test; active lifecycle artifacts are never compile-time fixtures. | `scope_adoption_fails_closed_when_anchor_is_unavailable_or_replayed` with `tests/fixtures/chg-0068-adopted-scope.json`. |
| `change/verification/scoped-review-child` | The final repository lane rejected fresh verification after the independent review was committed because verification freshness allowed its own persistence files but not the required review artifacts. | A review-only child may preserve verification only when every changed path is one of the exact review/verification persistence files for a canonical active change; any source or other path in the same child fails closed. | `scoped_review_persistence_commit_keeps_verification_current`; `scoped_review_persistence_mixed_with_source_change_is_stale`. |
| `change/verification/mutation-validator-split` | Native review recording and finalization checked only ancestry plus net digests, while project checking used the stricter every-parent descendant validator; a source change→revert could therefore regain matching bytes before mutation. | Workflow-v2 review recording and same-PR finalization call the same full descendant-history validator before recording review or adopting/finalizing evidence; legacy acceptance compatibility is unchanged. | `scoped_review_recording_rejects_verification_change_then_revert_history`; `scoped_review_freshness_rejects_change_then_revert_history`; legacy dangling-history and late-dependency acceptance fixtures. |
| `change/review/persisted-self-review` | Reviewer independence was enforced while writing but not while loading persisted attempts, so a manually introduced pass ledger could name the scope approver. | Every persisted attempt resolves the definition approval for its contract digest and rejects a case-insensitive reviewer/approver identity match before native finalization. | `persisted_scoped_review_rejects_scope_approver_as_reviewer`. |
