## ADDED

### REQUIREMENT REQ-cmd-check-009

`check` SHALL report an unmeasured tree as unmeasured in every format.

Acceptance Criteria
- Text, JSON, CSV, Markdown, GitHub and table all decline to print a percentage.
- JSON uses `null`.
