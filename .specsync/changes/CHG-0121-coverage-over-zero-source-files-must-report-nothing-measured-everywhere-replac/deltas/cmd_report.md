## ADDED

### REQUIREMENT REQ-cmd-report-002

`report` SHALL report an unmeasured tree as unmeasured in every format.

Acceptance Criteria
- Text, JSON, CSV and Markdown all decline to print a percentage; JSON uses `null`.
