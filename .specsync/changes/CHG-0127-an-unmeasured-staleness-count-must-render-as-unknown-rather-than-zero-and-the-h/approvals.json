{
  "approvals": [
    {
      "gate": "definition",
      "actor": "0xLeif",
      "timestamp": 1786755335,
      "digest": "f393208c5d4521aefbd9f679bdf8c73796ad7fed686e7209e9f7a94707c86c36",
      "note": null,
      "approved_scope": {
        "schema_version": 1,
        "change_id": "CHG-0127-an-unmeasured-staleness-count-must-render-as-unknown-rather-than-zero-and-the-h",
        "title": "An unmeasured staleness count must render as unknown rather than zero, and the hand-rolled config scanner must report a malformed header rather than silently skipping it",
        "description": "An unmeasured staleness count must render as unknown rather than zero, and the hand-rolled config scanner must report a malformed header rather than silently skipping it",
        "kind": "bug_fix",
        "affected_specs": [
          "cmd_report",
          "config"
        ],
        "affected_paths": [
          ".specsync/change-sequence.json",
          "CHANGELOG.md",
          "src/commands/report.rs",
          "src/config.rs",
          "tests/integration/staleness_unmeasurable.rs"
        ],
        "no_spec_change": false,
        "no_spec_change_rationale": null,
        "acceptance_criteria": [
          "On a tree whose staleness cannot be measured, report renders the stale count as unknown in text and null in JSON, never zero, because zero is an answer and there is none. A tree with real git history reports its actual count unchanged. A config file whose header is unterminated is reported as unloadable rather than silently skipped, using the same refusal wording as the unreadable-file shape, so a consumer matching a refusal need not know which door it came through. A well-formed config is unaffected, and valid TOML the scanner does not implement is not rejected."
        ],
        "dependencies": [],
        "supersedes": [],
        "answers": {
          "architecture_risk": "no",
          "public_contract": "yes"
        }
      }
    }
  ],
  "reopenings": []
}
