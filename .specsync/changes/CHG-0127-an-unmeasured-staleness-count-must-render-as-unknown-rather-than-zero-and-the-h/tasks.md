---
change: CHG-0127-an-unmeasured-staleness-count-must-render-as-unknown-rather-than-zero-and-the-h
artifact: tasks
---

# Tasks

1. `stale unknown` in the shared summary line; `null` in the JSON aggregate.
2. Record an unterminated header as `load_error`.
3. Unify the refusal wording across both shapes.
4. Correct the integration assertion that encoded `0` as acceptable.
