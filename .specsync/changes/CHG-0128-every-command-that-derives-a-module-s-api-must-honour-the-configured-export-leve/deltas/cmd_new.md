## ADDED

### REQUIREMENT REQ-cmd-new-003

`new` SHALL derive exports from the configured surface and parse mode.

Acceptance Criteria
- The Public API it writes contains only symbols `check` will accept.
