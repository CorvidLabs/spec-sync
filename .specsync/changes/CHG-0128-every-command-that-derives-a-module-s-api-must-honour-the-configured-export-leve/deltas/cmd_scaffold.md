## ADDED

### REQUIREMENT REQ-cmd-scaffold-003

`scaffold` SHALL derive exports from the configured surface and parse mode.

Acceptance Criteria
- The Public API it writes contains only symbols `check` will accept.
