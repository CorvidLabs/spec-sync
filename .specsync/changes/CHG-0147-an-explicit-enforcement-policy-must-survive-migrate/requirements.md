---
change: CHG-0147-an-explicit-enforcement-policy-must-survive-migrate
artifact: requirements
---

# Requirements

## REQ-config-012

Serialising configuration must state an explicit enforcement policy rather than
omitting it as equal to a default, so a project's effective policy cannot change
when the default does.
