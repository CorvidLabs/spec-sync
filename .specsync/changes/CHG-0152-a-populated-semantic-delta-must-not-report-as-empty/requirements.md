---
change: CHG-0152-a-populated-semantic-delta-must-not-report-as-empty
artifact: requirements
---

# Requirements

Adds `REQ-change-075`. The living statement is the semantic delta. A populated file with no
recognized operation heading must name the allowed headings instead of claiming emptiness;
the historical path must do the same; item headings match operation headings on case.
