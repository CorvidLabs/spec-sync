# Documentation

No user-facing documentation changes. This is an internal read pattern; the values derived and
every error surfaced are unchanged.

The reasoning is recorded where it is enforced: why the query is not cached, why `core.fsmonitor`
is excluded, and which git behaviours the single query had to be checked against.
