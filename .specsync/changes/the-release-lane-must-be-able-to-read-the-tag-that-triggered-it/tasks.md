# Tasks

- [x] Confirm the tag is annotated on the server, via the API rather than locally
- [x] Rule out a race between the tag push and the run
- [x] Establish that only `resolve` validates the tag object itself
- [x] Add `fetch-tags: true` with the measurement recorded beside it
